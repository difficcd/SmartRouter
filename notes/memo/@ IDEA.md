<!--
SPDX-FileCopyrightText: Copyright 2026 difficcd
SPDX-License-Identifier: Apache-2.0
-->


> router 개선 idea memo

`hash-regex`는 오라클의 **86.5%**. 다만 이 baseline은 Premium 비용비율이 3.985(한도의
99.6%)였고, **비공개 평가셋에서 4.2가 나와 Premium 0점**이 됐다. 즉 실전 점수는
0.695가 아니라 **0.473** — all-light보다 못하다.

=> 그렇다면 이런 경우에도 대응할 수 있게 수식을 짜는 건 어떤가?
각 모델별 고정 최대값을 지정해 준 다음, 그 경계 내부에서 ML적 할당해주면?

---

중성능과 저성능의 비용 차이
고성능과의 차이가 매우 큼.
1 2 24
이정도의 단위면 저수준 == 중수준 <<<<< 고수준 느낌.
1. 중수준이 애매하다
2. 저수준 대신 중수준을 쓸 정도로 중수준이 가성비가 좋다.

이렇게 해석 가능할 듯한데, 여기서도 새로운 아이디어를 도출해낼 수 있을 듯.
중수준을 어떻게 다루느냐.. 
중간단계모델의 취급에 따라 라우팅의 질을 높일 수 있을듯.

**비용은 정해진 한도 안에서, 점수(품질)는 최대한 높게.**
=> 비용 대비 성능 차이도 계산해야 할 것 같음
성능 차이 이미 있는지좀보자

=> 생각보다 더 중요할 거 같음.
light vs. high 분별은 비교적 수월하지만,
비용 대비 성능으로 light-mid 를 가르는 것이 힘들 것으로 보임.

항상 비용이 일정한게 아님 (하긴 그렇긴 함 모델 자체가 블랙박스니까.. 고정비용은 절대없겠지)
토큰을 매섭게 잡아먹으면서 score가 0점인 것을 가장 피해야 함.
그래서 light vs. high 분별이 중요하고
high 에 최대한 모든 심혈을 기울여야 함.
어떻게 해서 문장에서 high 특성을 추출할 것이고 검증할 것인가?


=> 일단 light vs. high 분별부터 학습시킨 다음
mid를 정교하게 갈고닦는 전략을 사용하는 것이 어떨까 싶네


---

라그랑주 승수법 식에서,
람다를 각 모델별로 따로따로 쓸 수 있는 구조라면?
high 를 엄격하게 평가하기 위해
high 의 람다만 좀더 높게 잡으면 되지 않을까?

그리고 
mid 가 애매하게 잡혀서 범위적으로는 합당한데 의미 맥락적으로는 불합당하다면
반올림/반내림 하는 기작으로도 처리 가능하지 않을까? 싶음.
반내림 하는게 합당해 보이긴 함. 올림은 막아두고 (high 는 엄격해야만 하니까)

**④ "라그랑주 식에서 모델별로 λ를 따로 쓸 수 있냐, high만 더 엄격하게?"** — **네, 되고 수학적으로도 안전합니다.** 증명을 짧게 드리면:

```
지금: EV(m) = q̂(m) − λ · ĉ(m) / light_total          (λ 하나)
변경: EV(m) = q̂(m) − λ · r[m] · ĉ(m) / light_total    (r[m] = 모델별 고정 위험계수, r[light]=1)
```

`r[m]`을 **미리 정한 상수**로만 쓰면(이분탐색 중엔 안 바뀜), 교본 08장의 단조성 증명이 그대로 성립합니다 — `λ`가 커질 때 `r[m]·ĉ(m)`도 같이 커지니까 "비쌀수록 더 크게 깎인다"가 유지되고, 이분탐색이 여전히 됩니다. 구현은 `hash_regex.select_models`의 `choose()` 안 한 줄만 바꾸면 됩니다: `costs[candidate]` → `RISK_MULTIPLIER[candidate] * costs[candidate]`. `r[axk1-think]`를 1보다 크게(예: 1.5) 잡으면 "k1은 실제 비용보다 더 비싸다고 치고 계산"하는 효과가 나서, 정확히 님이 원하는 "high를 엄격하게" 가 됩니다. `r[m]` 값 자체는 나중에 부트스트랩 실험(교본 10 §5)으로 데이터 기반으로 정하면 됩니다.

(반내림 기작은 어차피 이거랑 같은 도메인이라 복수 개의 람다로 통일하면 될듯.)


**"λ를 실행 중에 바뀌는 하이퍼파라미터로 할 수 없나?"** — **여기 규칙 위반 위험이 있어서 정확히 짚고 넘어가야 합니다.**

- **가능한 것 (이미 하고 있음)**: λ가 **입력 배치 전체를 한 번 보고 계산되는 값**인 건 괜찮습니다. `select_models`가 지금도 그렇게 합니다 — 880문항 전체의 예측 비용 합을 보고 이분탐색으로 λ 하나를 구합니다. 이건 "하이퍼파라미터가 아니라 배치마다 다시 계산되는 값"이라 이미 사실상 님이 원하는 것에 가깝습니다. => 이게 유의미한데 이미 되고있긴하네;

사실 순서에 따라서 그렇게 한다해도 학습데이터에대해서만 성능이 좋지
테스트데이터에서는 터지거나 점수가 덜나오거나 그럴 것임.

모델별 람다값을 어떻게 합리적으로 구하는가?
문제는, 지금 나한테 주어진 데이터셋이 880문항밖에 없다는 것임.
그래서  과적합 위험도 쉽고 (NN이라면) 여러가지 선택권이 없음.
"경향성" 분석에 초점을 맞춰야 할 것 같은데,
그 경향성을 어떻게 다루는지도 문제고..

세 가지 모델의 880문항 정답지가 있다..
ML로 다루기에는 데이터가 적고..
아래 방법들을 좀 보면


### ① 가중치 기반 수식 / 룰 기반 (Rule-based Scoring)

- **방식:** 쿼리의 길이, 키워드(예: "코드 작성", "번역", "요약"), 정규표현식, 복잡도 어휘 사전 등을 이용해 << 쿼리의 난이도 점수를 계산 >> 합니다.
    
- **특징:** 구현이 가장 단순하며 속도가 압도적으로 빠르고 리소스를 거의 쓰지 않습니다. << 그러나 문맥(Context)이나 뉘앙스를 파악하는 데 한계 >> 가 있습니다. => 당연하지... reg ex, 어휘사전, 길이, 키워드 정도로 되는거면 쉽지않을거임. 
    
### ② 전통적 머신러닝 분류기 (ML Classifier)

- **방식:** TF-IDF나 Bag-of-Words 같은 경량 텍스트 텍스트 피처 추출기를 거친 뒤, **Logistic Regression, SVM, Random Forest, XGBoost** 등의 전통적 ML 모델로 특정 쿼리를 어떤 LLM으로 보낼지 분류합니다.
    
- **특징:** 로컬 CPU 수준에서도 수 밀리초(ms) 단위로 동작하며, 학습 데이터를 통해 쿼리 유형별 최적의 모델 매핑 감각을 익힐 수 있습니다.
    

### ③ 임베딩 기반 코사인 유사도 / k-NN 검색

- **방식:** 로컬에서 가볍게 돌릴 수 있는 소형 센텐스 임베딩 모델(예: `bge-small`, `all-MiniLM-L6-v2` 등)을 이용해 쿼리를 벡터화합니다. << 이후 미리 정의된 각 LLM의 "강점 영역 벡터 cluster" 또는 과거 데이터셋과 유사도를 측정하여 최선의 모델을 선택 >> 합니다.
    
- **특징:** 키워드가 직접 일치하지 않아도 <<의미적(Semantic) 복잡도>> 를 판단할 수 있어 성능이 높습니다. => 이거는 좀 어려울 가능성이 높음. ML의 한계처럼..
    

### ④ 소형 슬림 분류용 SLM (Small Language Model)

- **방식:** 쿼리 분류만을 전문으로 하도록 fine-tuning된 아주 작은 로컬 분류 모델(예: Bert-base, RoBERTa-tiny 등)을 사용합니다.
    
- **특징:** 문맥 이해도가 높고 "이 문제는 어떤 모델이 가장 잘 푸는가"를 고차원적으로 추론해낼 수 있습니다.

# 

sLLM 파인튜닝을 해볼만할 것 같기도 한데?
파인튜닝으로 나온 결과를 람다값에 넣는다거나
프롬프트 자체만 파인튜닝 하는 것이 아니라
모델의 반응 자체를 파인튜닝한 모델을 
이 공모전에 집어넣을 수 있나? <= 이게 문제.
일단 오리엔테이션에서는 된다고 들었던 거 같기는 함.
외부 API를 사용하는 것은 아니니까..

=> **규칙상 됩니다, 단 조건이 있습니다.** `docs/CHALLENGE_RULES.md` 원문:
"AI 모델을 사용한다면 최소한 가중치를 공개한 모델이어야 합니다. 파인튜닝한 모델이나
직접 개발한 모델은 원본 라이선스가 허용하는 범위에서 별도 승인 없이 받을 수 있는
공개 위치에 가중치와 실행에 필요한 코드를 게시해야 합니다." + "일반 용도의 사전,
토크나이저와 적법하게 재배포할 수 있는 소형 언어 처리 모델도 네트워크 없이 자원
한도 안에서 사용할 수 있습니다."

조건 정리:
1. 베이스 모델이 이미 가중치 공개(Apache-2.0/MIT 등 상업이용 허용 라이선스)여야 함
2. 파인튜닝하면 **우리가 만든 가중치도 어딘가 공개 장소에 올려야 함**(비공개 저장 불가)
3. 네트워크 없이, `docs/RUNTIME.md` 자원 한도(2 CPU, 2GB RAM, **등급당 90초**) 안에서 실행
4. 라이선스가 상업적 이용·이미지 재배포·변형·평가 목적 사용을 허용해야 함

**그런데 지금 우선순위로는 추천 안 함**: 라벨이 Train 1,760개뿐이라 파인튜닝해도
과적합 위험이 ridge랑 똑같이 있고, 사전학습된 일반 언어이해가 "이 문제가 axk1-think를
헷갈리게 하는가"라는 이 대회 특유의 로컬한 패턴에 잘 전이된다는 보장도 없음(위
"SKT 모델 바운더리" 통찰과 같은 맥락). 거기다 패키징(가중치 공개 호스팅)·자원 한도
검증까지 새로 해야 해서 7일 안에는 지금 아는 병목(fast 등급 안전마진)을 놔두고
투자하기엔 리스크 대비 이득이 불확실함. **규칙 위반은 아니니 fast 등급 고치고 시간
남으면 시도할 후보로 보류.**

①~④ 중에서는 지금 하는 게 사실 ①(규칙기반 dense 특징)+②(해시 특징+선형모델)의
혼합입니다. ③(임베딩+코사인)은 별도 임베딩 모델을 이미지에 넣어야 해서 ④와 비슷한
투자 대비, 1,760개 라벨로 학습하는 우리 회귀보다 나을 근거가 약함 — 검증 없이는
채택 안 함.


---

**"Claude로 합성 데이터 만들어서 비공개셋처럼 써보자"** — 이건 지난번에 얘기한 "합성 라벨로 학습"이랑 **완전히 다른 얘기라 문제없습니다.** 

지난번 문제는 "이 프롬프트에 대한 진짜 점수를 합성으로 대체하려는 것"이었는데, 지금 아이디어는 **배분 알고리즘(안전계수)을 테스트하려고 가상의 비용·점수 분포를 만드는 것**입니다 — 프롬프트 내용과 무관하게 숫자만 가지고 노는 겁니다. 

이미 계획에 있던 부트스트랩 실험(교본 10 §5, 실측 Dev를 리샘플링)을 보완하는 좋은 방법입니다: 부트스트랩은 "실제 있었던 변동"만 재현하는데, 합성 최악 시나리오는 "아직 안 본 극단 상황"까지 넣어볼 수 있습니다. 둘을 같이 쓰는 게 맞는 방향입니다.

---

프롬프트 하나가 손으로 고른 특징 10개(길이, 한글비율, 코드/수식 기호 개수 등) + 단어를 해시로 흩뿌린 256개 칸, 총 266개 숫자로 바뀝니다.

=> 특징추출도 좀 더 다양한 dim으로 해서 각각 추출한 특징을 다른 방식으로 고차원적 매핑하는 것은 어떤가 (좀 더 입체적인 특징 추출로 과적합방지 및 학습기여)


예측 (`heuristic.py:2611~2635`) — 숫자 266개 → q̂, ĉ

**님이 고치고 싶어하는 비용예측(ĉ)이 정확히 이 `costs` 줄입니다.** 266개 숫자를 각 모델마다 따로 학습된 선형계수(266개)와 내적한 다음 `exp()`를 씌우는 게 전부입니다 — 학습(`training/train_router.py`)에서 `log(실제비용)`을 타깃으로 ridge 회귀를 돌렸으니, 예측할 때 반대로 `exp()`를 씌워 원래 스케일로 돌려놓는 겁니다.

**지금 이 예측이 얼마나 부정확한지**(교본 03 §3-4, 실험 5): 문항의 90%는 실제 비용의 2.5~2.6배 오차 안에 들어오는데, 나머지 10%는 그보다 훨씬 벗어납니다. 이게 fast 등급에서 안전마진을 다 갉아먹는 원인이고, **다음에 손댈 자리가 바로 이 266개 특징(주로 해시 256칸)과 계수를 학습하는 부분**입니다 — 특징을 더 좋게 만들거나, 학습 방식(`training/train_router.py`의 ridge 회귀)을 바꾸는 두 방향이 있습니다.

=> 단순한 가중치와의내적으로 바꾸는거면..
각 특성에 가중치를 더 부여해서 학습시키는 건?
특징을 좋게 만든다는건 전처리같은데 전처리를 한다면 어떻게 할 수 있을지도 궁금함.

---

프롬프트에 대한 모델 반응을 측정해야 하는 개인화된 과제라서 그런 건 알겠는데,
임베딩 모델을 통해 프롬프트의 특성추출을 더 잘해보는 방향은?
지금은 해시로 그냥 자리배정만 해주는거같던데,
한국어 단어 이미 훈련된 임베딩모델 가져와서 거기서 프롬프트 특성 추출한거로
다시 해보면 성능이 더 좋아지지 않을까?

=> 지금 해야할 곳의 IDEA

---

`messages`(멀티턴) 입력은 공식 규칙상 실전에서도 허용되는데 Train/Dev엔 단 한 건도 없어서(toy에만 존재), 라우터가 대응하는 것은 실측 확인했지만(`team_router_make_submission`으로 toy-002 통과) 예측 정확도는 검증 불가 — `episode_text`가 messages를 합칠 때 role(system/user/assistant)을 버리는 것도 잠재 손실 지점.